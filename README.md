1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
2. Answer: getElementById only select the id, and getElementsByClassName selects the class name and can select multiple classes. querySelector only select the frist class item but querySelectorAll select all the class item.
3. How to create and insert a new element into DOM?
4. answer: create element like: const newDiv = document.createElement("div"); than append to the body document.body.appendChild(newDiv);
5. What is Event Bubbling?
6. answer: Event Bubbling is revserse version  child > parent > body 
